---
"@banata-auth/nextjs": patch
---

Mirror `Set-Cookie` into `Set-Better-Auth-Cookie` on cross-origin auth proxy responses so hosted Banata auth flows can complete sign-up and sign-in against customer app Next.js route handlers.
