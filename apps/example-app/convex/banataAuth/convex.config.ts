/**
 * Banata Auth local component definition.
 *
 * This creates an isolated namespace for all auth tables.
 */
import { defineComponent } from "convex/server";

const component = defineComponent("banataAuth");
export default component;
