/**
 * Banata Auth component schema anchor.
 *
 * Convex's component-local codegen still expects a local schema module, so
 * this stays as a thin re-export of the packaged schema.
 */
export { default } from "@banata-auth/convex/schema";
