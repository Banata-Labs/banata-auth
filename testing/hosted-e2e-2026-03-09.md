# Hosted E2E Notes - 2026-03-09

## Scope

Validate the real external-consumer flow using published npm packages, the hosted Banata dashboard, and a fresh cloud Convex deployment.

## Pre-flight

- Public app integration was refactored to require a server-side Banata project API key.
- `apps/example-app` was converted from an embedded local Convex auth runtime into a pure consumer app.
- Hosted `auth-ui` retained an internal-only bypass for project-scoped auth pages so Banata's own first-party hosted surface still works without exposing customer API keys to the browser.

## Issues Found

### Pending

- None yet for the hosted npm-consumer pass.

## Notes

- Root `typecheck` passes.
- Root `lint` passes, with existing non-blocking dashboard complexity warnings.
- `packages/nextjs` tests pass.
- `apps/dashboard` tests pass.
- `apps/example-app` builds against the local package builds and warns, rather than crashes, when `BANATA_API_KEY` is missing during static build analysis.
